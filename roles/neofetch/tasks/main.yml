---

- name: Link cfgs
  file:
    src: "{{ role_path }}/files/neofetch/"
    dest: "{{ cfg_dir }}/neofetch"
    state: link

- name: Install
  become: true
  ansible.builtin.apt:
    state: latest
    name: neofetch
