#!/bin/sh

# remaps --- Configures keyboard settings.

# Commentary:

# This script should be called when starting the graphical environment;
# that is, `xprofile` (or equivalent).

# set key repeat delay and key repeat respectively
xset r rate 220 50

# altgr for accented letters, etc.
setxkbmap -layout 'us' -variant 'altgr-intl'

# map the CAPS-LOCK key to SUPER
setxkbmap -option 'caps:escape'
