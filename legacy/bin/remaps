#!/bin/sh

# remaps --- Configures keyboard settings.

# Commentary:

# Configures the keyboard variant, layout and other settings. This script
# should be called when starting the graphical environment.

# -----------------------------------------------------------------------------
# config
# -----------------------------------------------------------------------------
# '1' -> true ; blank -> false

kb_repeat_delay=220
kb_repeat_rate=50
kb_layout='us'
kb_variant='altgr-intl' # altgr for accented letters, etc.

# will remap to ESC
caps_lock_disable=1

# -----------------------------------------------------------------------------

xset r rate ${kb_repeat_delay} ${kb_repeat_rate}
setxkbmap -layout "${kb_layout}" -variant "${kb_variant}"

# map the CAPS-LOCK key to SUPER
test -n "${caps_lock_disable}" && setxkbmap -option 'caps:escape'

exit 0
