# -----------------------------------------------------------------------------
# workspaces
# -----------------------------------------------------------------------------

# super + n         - focus workspaces n
# super + shift + n - send focused node to workspace n
#
# where 'n' is a keyboard number from 1..9

super + {_,shift +} {1-9}
	bspc {desktop -f,node -d} '^{1-9}'

# -----------------------------------------------------------------------------
# nodes
# -----------------------------------------------------------------------------

# super + d         - switch focus to direction d
# super + shift + d - move focused node to direction d
#
# where 'd' is a direction (N/S/E/W) [vi binds]

super + {_,shift +} {h,j,k,l}
  bspc node -{f,s} {west,south,north,east}

# -----------------------------------------------------------------------------
# state
# -----------------------------------------------------------------------------
# chord: super + x

# c      - correctly closes the focused frame
# k      - forces killing the frame (unsafe, but sometimes required)
# f      - toggle full-screen
# space  - toggle floating
# r      - restart wm
# escape - quit wm

super + x ; {q,k}
  bspc node -{c,k}

super + x ; {f,space}
  bspc node -t '~{fullscreen,floating}'

super + x ; r
  bspc wm -r && pkill -USR1 'sxhkd'

super + x ; Escape
  bspc quit 0
