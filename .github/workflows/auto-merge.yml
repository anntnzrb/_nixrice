name: Auto-merge Dependabot

# Queues Dependabot PRs for merge; GitHub merges once the checks required on
# dev pass. Runs without checking out PR code, so pull_request_target's write
# token never touches untrusted content.
on: pull_request_target

permissions: {}

jobs:
  enable:
    if: github.event.pull_request.user.login == 'dependabot[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Enable auto-merge
        env:
          GH_TOKEN: ${{ github.token }}
          PR_URL: ${{ github.event.pull_request.html_url }}
        run: gh pr merge --auto --rebase "$PR_URL"
