_: {
  config.programs.rio.settings = {
    hide-cursor-when-typing = false;

    padding-x = 0;
    padding-y = [
      0
      0
    ];

    line-height = 1.0;

    cursor = {
      blinking = true;
      blinking-interval = 400;
    };
  };
}
