#!/bin/sh

# runemacs --- <desc>

# Commentary:

# <desc>

# run the Emacs as daemon if it's not already
! pgrep -x 'emacs' && emacs --daemon

# connect to the Emacs server
# ... if this is script is ran via terminal, run Emacs on it
tty > /dev/null 2>&1 && set -- --tty "${@}"

# ... else run graphical version
exec emacsclient -c "${@}"
