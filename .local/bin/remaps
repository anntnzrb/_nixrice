#!/bin/sh

# key-press repeat rate
xset r rate 225 50 &

# altgr + letter for (accents, etc)
setxkbmap -layout us -variant altgr-intl && pkill -15 "xcape"

# remaps CAPS-LOCK to ESCAPE
xcape -e 'Super_L=Escape' &
