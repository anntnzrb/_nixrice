#!/bin/sh

# mkscript --- A script that generates the skeleton of another (new) script.

# Commentary:

# Creates the file only if it does not exist in the current directory already.
# Makes it executable.

# checks
test -z "${1}" && printf 'You did not provide a name for the script.\n' >&2 \
    && exit 1
test -f "${1}" && printf 'The script already exists.\n' >&2 && exit 1

# create the script and its skeleton
touch "${1}"
chmod 0755 "${1}"
tee "${1}" <<! >/dev/null
#!/bin/sh

# $1 --- <desc>

# Commentary:

# <desc>

!
printf "The script '%s' has been created:\n" "${1}"
file "${1}"
