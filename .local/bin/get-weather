#!/bin/sh

# **
# retrieves tiny formatted weather info
#
# should adjust the varibale `LOCATION` to desired location
# *

# -----------------------------------------------------------------------------
# preamble
# -----------------------------------------------------------------------------

# load utilities
# shellcheck disable=1090
. "$LIBSHUTILS/std-utils"

# -----------------------------------------------------------------------------
# main
# -----------------------------------------------------------------------------

# set-up location
LOCATION="Guayaquil"

is_net && {
    curl -fs "wttr.in/$LOCATION?format=%c+%t" | tr -d '[:space:]'
} || printf '📡❌\n'
